'use strict';

const assert = require("assert");
const path = require("path");


// check every API request works
describe("#Check request API", () => {
    describe("#Check hacker-news firebase GET JSON API", () => {
        it("Should return JSON result", () => {
            // let file_names = await load_json_file_names();
            // for (let file_name of file_names) {
            //     let json_data = await load_json_data(path.resolve(examples_folder_path, file_name));
            //     assert.equal(true, is_json_string(json_data));
            // }
        });
    });
});
